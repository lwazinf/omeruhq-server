# Supabase — same DB as OmeruHQ-main and hq.omeru.io-main
DATABASE_URL=
DIRECT_URL=

# JWT signing secret (generate with: openssl rand -base64 32)
JWT_SECRET=

# Meta Cloud API — same credentials as the WhatsApp bot
WA_PHONE_NUMBER_ID=
WA_PERMANENT_TOKEN=
