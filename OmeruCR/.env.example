# ── Database (shared ecosystem Supabase Postgres) ──
DATABASE_URL="postgresql://..."
DIRECT_URL="postgresql://..."

# ── Session signing — generate with: openssl rand -base64 48 ──
CR_JWT_SECRET=""

# ── WhatsApp Cloud API (same values as OmeruWA) ──
WHATSAPP_PHONE_NUMBER_ID=""
WHATSAPP_ACCESS_TOKEN=""

# ── Platform mode mirror (chip in the CR header — admin-only visibility) ──
DEMO_MODE="false"   # keep in sync with OmeruWA's DEMO_MODE
